import { PrsimaClientExceptionFilter } from './prsima-client-exception.filter';

describe('PrsimaClientExceptionFilter', () => {
  it('should be defined', () => {
    expect(new PrsimaClientExceptionFilter()).toBeDefined();
  });
});
